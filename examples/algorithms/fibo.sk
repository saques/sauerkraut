/*
	Bad Fibonacci implementation, for comparison with dynfibo
*/

function fibo(n) {
	if (n == 1 || n == 0) {
		return n
	} else {
		return fibo(n - 1) + fibo(n - 2)
	}
}

print("Bad Fibonacci series: Enter N")
var n = read().toInt()
print(n.toString() + " fibonacci number: " + fibo(n))
