/*
	Factorial function.
	Calculates N!, where N is a number read from standard input.
*/
function fact(n) {
	if (n == 1) {
		return n;
	} else {
		return fact(n-1)*n
	}
	return a
}

print("Factorial function: Enter N")
var n = read().toInt()
print(n.toString() + "! = "+ fact(n))
